name: Build Gibson Containers

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_OSS: zero-day-ai/gibson-oss
  IMAGE_ENTERPRISE: zero-day-ai/gibson-enterprise

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      sdk: ${{ steps.versions.outputs.sdk }}
      gibson: ${{ steps.versions.outputs.gibson }}
      tools: ${{ steps.versions.outputs.tools }}
      agents: ${{ steps.versions.outputs.agents }}
      plugins: ${{ steps.versions.outputs.plugins }}
    steps:
      - uses: actions/checkout@v4
      - name: Parse versions
        id: versions
        run: |
          echo "sdk=$(jq -r '.sdk' versions.json)" >> $GITHUB_OUTPUT
          echo "gibson=$(jq -r '.gibson' versions.json)" >> $GITHUB_OUTPUT
          echo "tools=$(jq -r '.tools' versions.json)" >> $GITHUB_OUTPUT
          echo "agents=$(jq -r '.agents' versions.json)" >> $GITHUB_OUTPUT
          echo "plugins=$(jq -r '.plugins' versions.json)" >> $GITHUB_OUTPUT
      - name: Validate tags exist
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          # All repos are private - use gh api with PAT for authentication
          gh api repos/zero-day-ai/sdk/git/refs/tags/${{ steps.versions.outputs.sdk }} > /dev/null || (echo "SDK tag ${{ steps.versions.outputs.sdk }} not found" && exit 1)
          gh api repos/zero-day-ai/gibson/git/refs/tags/${{ steps.versions.outputs.gibson }} > /dev/null || (echo "Gibson tag ${{ steps.versions.outputs.gibson }} not found" && exit 1)
          gh api repos/zero-day-ai/gibson-tools-official/git/refs/tags/${{ steps.versions.outputs.tools }} > /dev/null || (echo "Tools tag ${{ steps.versions.outputs.tools }} not found" && exit 1)
          gh api repos/zero-day-ai/gibson-agents-official/git/refs/tags/${{ steps.versions.outputs.agents }} > /dev/null || (echo "Agents tag ${{ steps.versions.outputs.agents }} not found" && exit 1)
          gh api repos/zero-day-ai/gibson-plugins-official/git/refs/tags/${{ steps.versions.outputs.plugins }} > /dev/null || (echo "Plugins tag ${{ steps.versions.outputs.plugins }} not found" && exit 1)
          echo "All tags validated successfully"

  build-oss:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./build
          file: ./build/Dockerfile.oss
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_OSS }}:${{ needs.validate.outputs.gibson }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_OSS }}:latest
          build-args: |
            SDK_VERSION=${{ needs.validate.outputs.sdk }}
            GIBSON_VERSION=${{ needs.validate.outputs.gibson }}
            TOOLS_VERSION=${{ needs.validate.outputs.tools }}
          labels: |
            gibson.sdk.version=${{ needs.validate.outputs.sdk }}
            gibson.version=${{ needs.validate.outputs.gibson }}
            gibson.tools.version=${{ needs.validate.outputs.tools }}

  build-enterprise:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./build
          file: ./build/Dockerfile.enterprise
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_ENTERPRISE }}:${{ needs.validate.outputs.gibson }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_ENTERPRISE }}:latest
          build-args: |
            SDK_VERSION=${{ needs.validate.outputs.sdk }}
            GIBSON_VERSION=${{ needs.validate.outputs.gibson }}
            TOOLS_VERSION=${{ needs.validate.outputs.tools }}
            AGENTS_VERSION=${{ needs.validate.outputs.agents }}
            PLUGINS_VERSION=${{ needs.validate.outputs.plugins }}
            GITHUB_TOKEN=${{ secrets.PRIVATE_REPO_TOKEN }}
          labels: |
            gibson.sdk.version=${{ needs.validate.outputs.sdk }}
            gibson.version=${{ needs.validate.outputs.gibson }}
            gibson.tools.version=${{ needs.validate.outputs.tools }}
            gibson.agents.version=${{ needs.validate.outputs.agents }}
            gibson.plugins.version=${{ needs.validate.outputs.plugins }}
