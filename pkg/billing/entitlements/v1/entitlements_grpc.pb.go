// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.0
// - protoc             (unknown)
// source: entitlements.proto

// Package gibson.billing.entitlements.v1 defines the EntitlementsService —
// the gRPC network seam between the OSS gibson daemon and the closed
// commercial billing service.
//
// Architecture context (ADR-0003 / ADR-0054 / gibson#1026 / gibson#1027):
// The entitlements seam converts from compile-time injection (Option A,
// billing repo imports gibson's pkg/billing/entitlements.Provider) to a
// runtime gRPC call (Option B). The OSS daemon ships a config-gated caching
// gRPC client (EntitlementsServiceClient); the closed billing repo ships a
// small server (EntitlementsServiceServer) that derives limits from the plan
// + Stripe subscription state.
//
// Boundary rules:
//   - This proto lives under pkg/ (not internal/) so the external
//     github.com/zeroroot-ai/billing module can import the generated stubs
//     to implement the server side without violating Go's internal/ import
//     restriction.
//   - gibson NEVER imports github.com/zeroroot-ai/billing. The boundary is
//     strictly one-way: billing → gibson/pkg/billing/entitlements/v1.
//   - This RPC is daemon→billing over SPIFFE mTLS. It is NOT routed through
//     Envoy / ext-authz, so no (gibson.auth.v1.authz) annotation is needed
//     and this file is NOT included in the authz-registry codegen pipeline.

package entitlementsv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	EntitlementsService_GetLimits_FullMethodName = "/gibson.billing.entitlements.v1.EntitlementsService/GetLimits"
)

// EntitlementsServiceClient is the client API for EntitlementsService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// EntitlementsService answers "what are this tenant's resource limits?" for
// the OSS daemon. The daemon calls GetLimits on each enforcement decision;
// the billing service derives the answer from the tenant's plan + subscription
// state. A missing or unreachable billing service causes the daemon to fall
// back to the UnlimitedProvider (fail-open, matching OSS behaviour).
type EntitlementsServiceClient interface {
	// GetLimits returns the resource ceilings for the given tenant. The billing
	// service derives these from the tenant's active plan + subscription. A
	// tenant with no active subscription or on a free plan returns zero on every
	// field (unlimited). The daemon caches the result with a configurable TTL
	// (default 60 s) to minimise round-trips on high-frequency enforcement
	// points (concurrency checks, budget checks).
	GetLimits(ctx context.Context, in *GetLimitsRequest, opts ...grpc.CallOption) (*Limits, error)
}

type entitlementsServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewEntitlementsServiceClient(cc grpc.ClientConnInterface) EntitlementsServiceClient {
	return &entitlementsServiceClient{cc}
}

func (c *entitlementsServiceClient) GetLimits(ctx context.Context, in *GetLimitsRequest, opts ...grpc.CallOption) (*Limits, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Limits)
	err := c.cc.Invoke(ctx, EntitlementsService_GetLimits_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// EntitlementsServiceServer is the server API for EntitlementsService service.
// All implementations must embed UnimplementedEntitlementsServiceServer
// for forward compatibility.
//
// EntitlementsService answers "what are this tenant's resource limits?" for
// the OSS daemon. The daemon calls GetLimits on each enforcement decision;
// the billing service derives the answer from the tenant's plan + subscription
// state. A missing or unreachable billing service causes the daemon to fall
// back to the UnlimitedProvider (fail-open, matching OSS behaviour).
type EntitlementsServiceServer interface {
	// GetLimits returns the resource ceilings for the given tenant. The billing
	// service derives these from the tenant's active plan + subscription. A
	// tenant with no active subscription or on a free plan returns zero on every
	// field (unlimited). The daemon caches the result with a configurable TTL
	// (default 60 s) to minimise round-trips on high-frequency enforcement
	// points (concurrency checks, budget checks).
	GetLimits(context.Context, *GetLimitsRequest) (*Limits, error)
	mustEmbedUnimplementedEntitlementsServiceServer()
}

// UnimplementedEntitlementsServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEntitlementsServiceServer struct{}

func (UnimplementedEntitlementsServiceServer) GetLimits(context.Context, *GetLimitsRequest) (*Limits, error) {
	return nil, status.Error(codes.Unimplemented, "method GetLimits not implemented")
}
func (UnimplementedEntitlementsServiceServer) mustEmbedUnimplementedEntitlementsServiceServer() {}
func (UnimplementedEntitlementsServiceServer) testEmbeddedByValue()                             {}

// UnsafeEntitlementsServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EntitlementsServiceServer will
// result in compilation errors.
type UnsafeEntitlementsServiceServer interface {
	mustEmbedUnimplementedEntitlementsServiceServer()
}

func RegisterEntitlementsServiceServer(s grpc.ServiceRegistrar, srv EntitlementsServiceServer) {
	// If the following call panics, it indicates UnimplementedEntitlementsServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&EntitlementsService_ServiceDesc, srv)
}

func _EntitlementsService_GetLimits_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetLimitsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EntitlementsServiceServer).GetLimits(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EntitlementsService_GetLimits_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EntitlementsServiceServer).GetLimits(ctx, req.(*GetLimitsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// EntitlementsService_ServiceDesc is the grpc.ServiceDesc for EntitlementsService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var EntitlementsService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gibson.billing.entitlements.v1.EntitlementsService",
	HandlerType: (*EntitlementsServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetLimits",
			Handler:    _EntitlementsService_GetLimits_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "entitlements.proto",
}
