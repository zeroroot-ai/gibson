// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.0
// - protoc             (unknown)
// source: gibson/tenant/v1/connector_auth.proto

// Package gibson.tenant.v1 — ConnectorAuthService: the dashboard-facing
// surface for a connector's OAuth grant lifecycle (ADR-0064).
//
// The human authorization round trip runs in the operator's browser against
// the customer's own vendor instance; what reaches this service is the
// completed grant. The refresh token crosses this API exactly once, on
// CompleteConnectorAuthorization, and is never returned by any RPC.
//
// Authorization: every RPC carries a (gibson.auth.v1.authz) annotation.

package tenantv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ConnectorAuthService_GetConnectorAuthStatus_FullMethodName         = "/gibson.tenant.v1.ConnectorAuthService/GetConnectorAuthStatus"
	ConnectorAuthService_StartConnectorAuthorization_FullMethodName    = "/gibson.tenant.v1.ConnectorAuthService/StartConnectorAuthorization"
	ConnectorAuthService_CompleteConnectorAuthorization_FullMethodName = "/gibson.tenant.v1.ConnectorAuthService/CompleteConnectorAuthorization"
	ConnectorAuthService_RevokeConnectorGrant_FullMethodName           = "/gibson.tenant.v1.ConnectorAuthService/RevokeConnectorGrant"
	ConnectorAuthService_SetConnectorSecret_FullMethodName             = "/gibson.tenant.v1.ConnectorAuthService/SetConnectorSecret"
)

// ConnectorAuthServiceClient is the client API for ConnectorAuthService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ConnectorAuthService manages the OAuth grant behind a connector.
//
// A connector is a service account with an audit trail in front of it: the
// grant records which human authorized it, and that human is surfaced next to
// the connector in the dashboard. The platform's refresher mints the
// short-lived access token the connector presents; the connector can never
// resolve the grant itself.
type ConnectorAuthServiceClient interface {
	// GetConnectorAuthStatus reports whether a connector has a usable grant,
	// who authorized it, and the state of its published access token. It never
	// returns credential material.
	GetConnectorAuthStatus(ctx context.Context, in *GetConnectorAuthStatusRequest, opts ...grpc.CallOption) (*GetConnectorAuthStatusResponse, error)
	// StartConnectorAuthorization begins an OAuth authorization for a connector
	// against the customer's own vendor instance. The daemon runs RFC 8414
	// discovery and RFC 7591 dynamic client registration, mints a PKCE pair and
	// a random state, holds them in a short-TTL server-side store keyed by
	// state, and returns the authorize URL the operator opens. The browser round
	// trip returns to the daemon callback, so no human pastes a URL and no local
	// listener runs by hand (ADR-0014).
	StartConnectorAuthorization(ctx context.Context, in *StartConnectorAuthorizationRequest, opts ...grpc.CallOption) (*StartConnectorAuthorizationResponse, error)
	// CompleteConnectorAuthorization finishes the authorization the daemon
	// started: it takes the authorization code and the state that binds it, does
	// the code-to-token exchange daemon-side, stores the grant, and immediately
	// exercises the refresh_token grant against the vendor to prove it works and
	// publish the first access token. A grant that cannot refresh is not stored:
	// the operator learns at authorization time, not two hours later. The
	// refresh token never leaves the daemon.
	//
	// Re-authorizing replaces the previous grant in place. The daemon callback
	// runs the same path, so completing through the browser and completing
	// through this RPC are one behaviour.
	CompleteConnectorAuthorization(ctx context.Context, in *CompleteConnectorAuthorizationRequest, opts ...grpc.CallOption) (*CompleteConnectorAuthorizationResponse, error)
	// RevokeConnectorGrant revokes the connector's grant: best-effort
	// revocation at the vendor, then deletion of the grant and the published
	// access token. Takes effect for every agent using the connector, because
	// the platform holds the only credential. Idempotent. A static credential
	// (SetConnectorSecret) is removed the same way, with no vendor call.
	RevokeConnectorGrant(ctx context.Context, in *RevokeConnectorGrantRequest, opts ...grpc.CallOption) (*RevokeConnectorGrantResponse, error)
	// SetConnectorSecret stores a customer-supplied static credential (for
	// example a personal access token) for an `auth: secret` connector
	// (ADR-0015). The daemon writes it to the tenant's configured secret store
	// (the hosted OpenBao namespace or the customer's BYO Vault) as the
	// connector's access secret, and records a static grant that names the
	// human who supplied it. The materializer then publishes it to the
	// ToolHive proxy exactly as it does an OAuth access token. The credential
	// crosses the API once, inbound; no RPC ever returns it. Calling it again
	// replaces the credential in place.
	SetConnectorSecret(ctx context.Context, in *SetConnectorSecretRequest, opts ...grpc.CallOption) (*SetConnectorSecretResponse, error)
}

type connectorAuthServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewConnectorAuthServiceClient(cc grpc.ClientConnInterface) ConnectorAuthServiceClient {
	return &connectorAuthServiceClient{cc}
}

func (c *connectorAuthServiceClient) GetConnectorAuthStatus(ctx context.Context, in *GetConnectorAuthStatusRequest, opts ...grpc.CallOption) (*GetConnectorAuthStatusResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetConnectorAuthStatusResponse)
	err := c.cc.Invoke(ctx, ConnectorAuthService_GetConnectorAuthStatus_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorAuthServiceClient) StartConnectorAuthorization(ctx context.Context, in *StartConnectorAuthorizationRequest, opts ...grpc.CallOption) (*StartConnectorAuthorizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(StartConnectorAuthorizationResponse)
	err := c.cc.Invoke(ctx, ConnectorAuthService_StartConnectorAuthorization_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorAuthServiceClient) CompleteConnectorAuthorization(ctx context.Context, in *CompleteConnectorAuthorizationRequest, opts ...grpc.CallOption) (*CompleteConnectorAuthorizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CompleteConnectorAuthorizationResponse)
	err := c.cc.Invoke(ctx, ConnectorAuthService_CompleteConnectorAuthorization_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorAuthServiceClient) RevokeConnectorGrant(ctx context.Context, in *RevokeConnectorGrantRequest, opts ...grpc.CallOption) (*RevokeConnectorGrantResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RevokeConnectorGrantResponse)
	err := c.cc.Invoke(ctx, ConnectorAuthService_RevokeConnectorGrant_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *connectorAuthServiceClient) SetConnectorSecret(ctx context.Context, in *SetConnectorSecretRequest, opts ...grpc.CallOption) (*SetConnectorSecretResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SetConnectorSecretResponse)
	err := c.cc.Invoke(ctx, ConnectorAuthService_SetConnectorSecret_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ConnectorAuthServiceServer is the server API for ConnectorAuthService service.
// All implementations must embed UnimplementedConnectorAuthServiceServer
// for forward compatibility.
//
// ConnectorAuthService manages the OAuth grant behind a connector.
//
// A connector is a service account with an audit trail in front of it: the
// grant records which human authorized it, and that human is surfaced next to
// the connector in the dashboard. The platform's refresher mints the
// short-lived access token the connector presents; the connector can never
// resolve the grant itself.
type ConnectorAuthServiceServer interface {
	// GetConnectorAuthStatus reports whether a connector has a usable grant,
	// who authorized it, and the state of its published access token. It never
	// returns credential material.
	GetConnectorAuthStatus(context.Context, *GetConnectorAuthStatusRequest) (*GetConnectorAuthStatusResponse, error)
	// StartConnectorAuthorization begins an OAuth authorization for a connector
	// against the customer's own vendor instance. The daemon runs RFC 8414
	// discovery and RFC 7591 dynamic client registration, mints a PKCE pair and
	// a random state, holds them in a short-TTL server-side store keyed by
	// state, and returns the authorize URL the operator opens. The browser round
	// trip returns to the daemon callback, so no human pastes a URL and no local
	// listener runs by hand (ADR-0014).
	StartConnectorAuthorization(context.Context, *StartConnectorAuthorizationRequest) (*StartConnectorAuthorizationResponse, error)
	// CompleteConnectorAuthorization finishes the authorization the daemon
	// started: it takes the authorization code and the state that binds it, does
	// the code-to-token exchange daemon-side, stores the grant, and immediately
	// exercises the refresh_token grant against the vendor to prove it works and
	// publish the first access token. A grant that cannot refresh is not stored:
	// the operator learns at authorization time, not two hours later. The
	// refresh token never leaves the daemon.
	//
	// Re-authorizing replaces the previous grant in place. The daemon callback
	// runs the same path, so completing through the browser and completing
	// through this RPC are one behaviour.
	CompleteConnectorAuthorization(context.Context, *CompleteConnectorAuthorizationRequest) (*CompleteConnectorAuthorizationResponse, error)
	// RevokeConnectorGrant revokes the connector's grant: best-effort
	// revocation at the vendor, then deletion of the grant and the published
	// access token. Takes effect for every agent using the connector, because
	// the platform holds the only credential. Idempotent. A static credential
	// (SetConnectorSecret) is removed the same way, with no vendor call.
	RevokeConnectorGrant(context.Context, *RevokeConnectorGrantRequest) (*RevokeConnectorGrantResponse, error)
	// SetConnectorSecret stores a customer-supplied static credential (for
	// example a personal access token) for an `auth: secret` connector
	// (ADR-0015). The daemon writes it to the tenant's configured secret store
	// (the hosted OpenBao namespace or the customer's BYO Vault) as the
	// connector's access secret, and records a static grant that names the
	// human who supplied it. The materializer then publishes it to the
	// ToolHive proxy exactly as it does an OAuth access token. The credential
	// crosses the API once, inbound; no RPC ever returns it. Calling it again
	// replaces the credential in place.
	SetConnectorSecret(context.Context, *SetConnectorSecretRequest) (*SetConnectorSecretResponse, error)
	mustEmbedUnimplementedConnectorAuthServiceServer()
}

// UnimplementedConnectorAuthServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedConnectorAuthServiceServer struct{}

func (UnimplementedConnectorAuthServiceServer) GetConnectorAuthStatus(context.Context, *GetConnectorAuthStatusRequest) (*GetConnectorAuthStatusResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetConnectorAuthStatus not implemented")
}
func (UnimplementedConnectorAuthServiceServer) StartConnectorAuthorization(context.Context, *StartConnectorAuthorizationRequest) (*StartConnectorAuthorizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method StartConnectorAuthorization not implemented")
}
func (UnimplementedConnectorAuthServiceServer) CompleteConnectorAuthorization(context.Context, *CompleteConnectorAuthorizationRequest) (*CompleteConnectorAuthorizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CompleteConnectorAuthorization not implemented")
}
func (UnimplementedConnectorAuthServiceServer) RevokeConnectorGrant(context.Context, *RevokeConnectorGrantRequest) (*RevokeConnectorGrantResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RevokeConnectorGrant not implemented")
}
func (UnimplementedConnectorAuthServiceServer) SetConnectorSecret(context.Context, *SetConnectorSecretRequest) (*SetConnectorSecretResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method SetConnectorSecret not implemented")
}
func (UnimplementedConnectorAuthServiceServer) mustEmbedUnimplementedConnectorAuthServiceServer() {}
func (UnimplementedConnectorAuthServiceServer) testEmbeddedByValue()                              {}

// UnsafeConnectorAuthServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ConnectorAuthServiceServer will
// result in compilation errors.
type UnsafeConnectorAuthServiceServer interface {
	mustEmbedUnimplementedConnectorAuthServiceServer()
}

func RegisterConnectorAuthServiceServer(s grpc.ServiceRegistrar, srv ConnectorAuthServiceServer) {
	// If the following call panics, it indicates UnimplementedConnectorAuthServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ConnectorAuthService_ServiceDesc, srv)
}

func _ConnectorAuthService_GetConnectorAuthStatus_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetConnectorAuthStatusRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorAuthServiceServer).GetConnectorAuthStatus(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorAuthService_GetConnectorAuthStatus_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorAuthServiceServer).GetConnectorAuthStatus(ctx, req.(*GetConnectorAuthStatusRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorAuthService_StartConnectorAuthorization_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(StartConnectorAuthorizationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorAuthServiceServer).StartConnectorAuthorization(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorAuthService_StartConnectorAuthorization_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorAuthServiceServer).StartConnectorAuthorization(ctx, req.(*StartConnectorAuthorizationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorAuthService_CompleteConnectorAuthorization_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CompleteConnectorAuthorizationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorAuthServiceServer).CompleteConnectorAuthorization(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorAuthService_CompleteConnectorAuthorization_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorAuthServiceServer).CompleteConnectorAuthorization(ctx, req.(*CompleteConnectorAuthorizationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorAuthService_RevokeConnectorGrant_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeConnectorGrantRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorAuthServiceServer).RevokeConnectorGrant(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorAuthService_RevokeConnectorGrant_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorAuthServiceServer).RevokeConnectorGrant(ctx, req.(*RevokeConnectorGrantRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ConnectorAuthService_SetConnectorSecret_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SetConnectorSecretRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ConnectorAuthServiceServer).SetConnectorSecret(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ConnectorAuthService_SetConnectorSecret_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ConnectorAuthServiceServer).SetConnectorSecret(ctx, req.(*SetConnectorSecretRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// ConnectorAuthService_ServiceDesc is the grpc.ServiceDesc for ConnectorAuthService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ConnectorAuthService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gibson.tenant.v1.ConnectorAuthService",
	HandlerType: (*ConnectorAuthServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetConnectorAuthStatus",
			Handler:    _ConnectorAuthService_GetConnectorAuthStatus_Handler,
		},
		{
			MethodName: "StartConnectorAuthorization",
			Handler:    _ConnectorAuthService_StartConnectorAuthorization_Handler,
		},
		{
			MethodName: "CompleteConnectorAuthorization",
			Handler:    _ConnectorAuthService_CompleteConnectorAuthorization_Handler,
		},
		{
			MethodName: "RevokeConnectorGrant",
			Handler:    _ConnectorAuthService_RevokeConnectorGrant_Handler,
		},
		{
			MethodName: "SetConnectorSecret",
			Handler:    _ConnectorAuthService_SetConnectorSecret_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "gibson/tenant/v1/connector_auth.proto",
}
