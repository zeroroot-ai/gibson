// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.0
// - protoc             (unknown)
// source: gibson/daemon/logs/v1/logs.proto

package logsv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	LogsService_QueryMissionLogs_FullMethodName = "/gibson.daemon.logs.v1.LogsService/QueryMissionLogs"
	LogsService_QueryDaemonLogs_FullMethodName  = "/gibson.daemon.logs.v1.LogsService/QueryDaemonLogs"
)

// LogsServiceClient is the client API for LogsService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// LogsService is the daemon-mediated read path into tenant-scoped mission logs
// and events stored in Loki (E9, gibson#811). The daemon resolves the caller's
// tenant from the authenticated context and constructs the Loki query's
// `tenant_id` label + `X-Scope-OrgID` org server-side — the dashboard never
// fetches Loki directly and never supplies a tenant scope. This is the
// tenant-isolation fix: previously the dashboard's loki-client.ts built the
// `X-Scope-OrgID` header and the `tenant_id` LogQL filter from a client-held
// value; now the daemon owns it, so a caller can never read another tenant's
// logs. It mirrors WorldService / TracesService exactly: daemon-local,
// tenant-scoped observability over a backing store, read through Envoy +
// ext-authz.
//
// Every RPC is ext-authz-gated via the tenant member relation.
type LogsServiceClient interface {
	// QueryMissionLogs returns the tenant's log lines for a single mission. The
	// mission_id is matched within the tenant's log stream; the tenant scope is
	// derived server-side from the authenticated identity, never supplied by the
	// client.
	QueryMissionLogs(ctx context.Context, in *QueryMissionLogsRequest, opts ...grpc.CallOption) (*QueryMissionLogsResponse, error)
	// QueryDaemonLogs returns the tenant's daemon log lines, optionally filtered
	// by level and/or mission_id. The tenant scope is derived server-side.
	QueryDaemonLogs(ctx context.Context, in *QueryDaemonLogsRequest, opts ...grpc.CallOption) (*QueryDaemonLogsResponse, error)
}

type logsServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewLogsServiceClient(cc grpc.ClientConnInterface) LogsServiceClient {
	return &logsServiceClient{cc}
}

func (c *logsServiceClient) QueryMissionLogs(ctx context.Context, in *QueryMissionLogsRequest, opts ...grpc.CallOption) (*QueryMissionLogsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(QueryMissionLogsResponse)
	err := c.cc.Invoke(ctx, LogsService_QueryMissionLogs_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *logsServiceClient) QueryDaemonLogs(ctx context.Context, in *QueryDaemonLogsRequest, opts ...grpc.CallOption) (*QueryDaemonLogsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(QueryDaemonLogsResponse)
	err := c.cc.Invoke(ctx, LogsService_QueryDaemonLogs_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// LogsServiceServer is the server API for LogsService service.
// All implementations must embed UnimplementedLogsServiceServer
// for forward compatibility.
//
// LogsService is the daemon-mediated read path into tenant-scoped mission logs
// and events stored in Loki (E9, gibson#811). The daemon resolves the caller's
// tenant from the authenticated context and constructs the Loki query's
// `tenant_id` label + `X-Scope-OrgID` org server-side — the dashboard never
// fetches Loki directly and never supplies a tenant scope. This is the
// tenant-isolation fix: previously the dashboard's loki-client.ts built the
// `X-Scope-OrgID` header and the `tenant_id` LogQL filter from a client-held
// value; now the daemon owns it, so a caller can never read another tenant's
// logs. It mirrors WorldService / TracesService exactly: daemon-local,
// tenant-scoped observability over a backing store, read through Envoy +
// ext-authz.
//
// Every RPC is ext-authz-gated via the tenant member relation.
type LogsServiceServer interface {
	// QueryMissionLogs returns the tenant's log lines for a single mission. The
	// mission_id is matched within the tenant's log stream; the tenant scope is
	// derived server-side from the authenticated identity, never supplied by the
	// client.
	QueryMissionLogs(context.Context, *QueryMissionLogsRequest) (*QueryMissionLogsResponse, error)
	// QueryDaemonLogs returns the tenant's daemon log lines, optionally filtered
	// by level and/or mission_id. The tenant scope is derived server-side.
	QueryDaemonLogs(context.Context, *QueryDaemonLogsRequest) (*QueryDaemonLogsResponse, error)
	mustEmbedUnimplementedLogsServiceServer()
}

// UnimplementedLogsServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedLogsServiceServer struct{}

func (UnimplementedLogsServiceServer) QueryMissionLogs(context.Context, *QueryMissionLogsRequest) (*QueryMissionLogsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method QueryMissionLogs not implemented")
}
func (UnimplementedLogsServiceServer) QueryDaemonLogs(context.Context, *QueryDaemonLogsRequest) (*QueryDaemonLogsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method QueryDaemonLogs not implemented")
}
func (UnimplementedLogsServiceServer) mustEmbedUnimplementedLogsServiceServer() {}
func (UnimplementedLogsServiceServer) testEmbeddedByValue()                     {}

// UnsafeLogsServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to LogsServiceServer will
// result in compilation errors.
type UnsafeLogsServiceServer interface {
	mustEmbedUnimplementedLogsServiceServer()
}

func RegisterLogsServiceServer(s grpc.ServiceRegistrar, srv LogsServiceServer) {
	// If the following call panics, it indicates UnimplementedLogsServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&LogsService_ServiceDesc, srv)
}

func _LogsService_QueryMissionLogs_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(QueryMissionLogsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(LogsServiceServer).QueryMissionLogs(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: LogsService_QueryMissionLogs_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(LogsServiceServer).QueryMissionLogs(ctx, req.(*QueryMissionLogsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _LogsService_QueryDaemonLogs_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(QueryDaemonLogsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(LogsServiceServer).QueryDaemonLogs(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: LogsService_QueryDaemonLogs_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(LogsServiceServer).QueryDaemonLogs(ctx, req.(*QueryDaemonLogsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// LogsService_ServiceDesc is the grpc.ServiceDesc for LogsService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var LogsService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gibson.daemon.logs.v1.LogsService",
	HandlerType: (*LogsServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "QueryMissionLogs",
			Handler:    _LogsService_QueryMissionLogs_Handler,
		},
		{
			MethodName: "QueryDaemonLogs",
			Handler:    _LogsService_QueryDaemonLogs_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "gibson/daemon/logs/v1/logs.proto",
}
