// Phase D: Neo4j per-tenant schema migrations will be added here.
// Each tenant's Neo4j database receives the same constraint/index set.
// No tenant_id property — the per-tenant database is the isolation boundary.
