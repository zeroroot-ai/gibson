# This rule is not used by the project tenant-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to gibson.zeroroot.ai resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: tenant-operator
    app.kubernetes.io/managed-by: kustomize
  name: tenantmember-viewer-role
rules:
- apiGroups:
  - gibson.zeroroot.ai
  resources:
  - tenantmembers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - gibson.zeroroot.ai
  resources:
  - tenantmembers/status
  verbs:
  - get
